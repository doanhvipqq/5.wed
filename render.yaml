services:
  - type: web
    name: bongx-ai-web
    env: python
    region: singapore
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web.app:app
    envVars:
      - key: CER_API_KEY
        sync: false
      - key: PORT
        value: 10000
